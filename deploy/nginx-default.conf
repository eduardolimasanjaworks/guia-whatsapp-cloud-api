server {
    listen 80;
    server_name localhost;
    root /usr/share/nginx/html;
    index guia-visual-api-oficial.html;

    location / {
        try_files $uri $uri/ /guia-visual-api-oficial.html;
    }

    location ~* \.md$ {
        return 404;
    }
}
